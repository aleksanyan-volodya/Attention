"""
Multi-Emotion Recognition with Transformer (Encoder-Only).

This package extends the binary sentiment classifier in `emotions/`
to recognize multiple distinct emotions from text (e.g. joy, sadness,
anger, fear, surprise, disgust, ...).

Right now this is a template with the same structure as `emotions/`, but
maybe it can use shared files after in the future.
"""

__version__ = "1.0.0"
